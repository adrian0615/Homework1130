// creating a strtupzer function of the String type that counts vowels in order
// and also removes every odd vowel from the returning String

enum Vowels {
    case a(Int)
    case e(Int)
    case i(Int)
    case o(Int)
    case u(Int)
}


func strtupzer(n: Int) -> String {
    